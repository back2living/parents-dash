// const useFormHandler = (form: any) => {
//     const { formState, register, handleSubmit, watch } = form;
//     const { errors, isValid, isSubmitting } = formState;
//
//     return { formState, register, handleSubmit, watch, errors, isValid, isSubmitting };
// };